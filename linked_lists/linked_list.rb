class Linked_List
   def append(value)
      # add node to end
   end

   def prepend(value)
      # add node to start
   end

   def size
      # total number of nodes
   end

   def head
      # return first node
   end

   def tail
      # return last node
   end

   def at(index)
      # return spec node
   end

   def pop
      # remove last node
   end

   def contains?(value)
      # true if value is in any node, false otherwise
   end

   def find(value)
      # index if value is in node (return multiple?), nil otherwise
   end

   def to_s
      # string. format:
      # ( value ) -> ( value ) -> ( value ) -> nil
   end

   # extra credit: [insert_at(value, index)] and [remove_at(index)]
end